version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: genealogy-api:latest
    container_name: genealogy-api
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    restart: unless-stopped
